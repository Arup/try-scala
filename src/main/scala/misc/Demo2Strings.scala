package misc;
object Demo2Strings{
        def main(args:Array[String]):Unit={
        var res=""
        val x=20;
        if(x==20)
        res=("x=20")
        else
        res=("x is not 20")
        println(res)

        val res2=0
                if(x==20) println("x==20")
                else
                        println("x!=20")
        println(res2)
        }
        }
